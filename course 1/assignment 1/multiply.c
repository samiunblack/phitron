#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    long long a, b;
        scanf("%lld %lld", &a, &b);
    long long multiply = a * b;

    printf("%lld", multiply);   
    return 0;
}
